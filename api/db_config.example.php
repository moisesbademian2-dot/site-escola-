<?php
// Copy this file to db_config.php (same folder) and fill in your real credentials.
// db_config.php is gitignored, so it's never committed. Without it, config.php
// falls back to a fresh-XAMPP default (root user, no password) automatically.

define('DB_HOST', '127.0.0.1');
define('DB_NAME', 'portal_of_future');
define('DB_USER', 'root');
define('DB_PASS', '');
